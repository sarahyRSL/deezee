<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ app.py/$1 [QSA,L]
</IfModule>

PassengerEnabled on 
PassengerAppRoot / 
PassengerPython /deezee/venv/bin/python